spring.application.name=hospital
server.port=${PORT:8088}

# MongoDB Database Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/nirmala_hospital}

# SMTP Mail Configuration
MAIL_HOST_VAL=${MAIL_HOST:smtp.gmail.com}
MAIL_PORT_VAL=${MAIL_PORT:587}
MAIL_USER_VAL=${MAIL_USER:nirmalaneurocarevzm@gmail.com}
MAIL_PASSWORD_VAL=${MAIL_PASSWORD:cmgvlehbcoyukvop}
MAIL_FROM_VAL=${MAIL_FROM:nirmalaneurocarevzm@gmail.com}

spring.mail.host=${MAIL_HOST_VAL}
spring.mail.port=${MAIL_PORT_VAL}
spring.mail.username=${MAIL_USER_VAL}
spring.mail.password=${MAIL_PASSWORD_VAL}
spring.mail.test-connection=false
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

# JWT Configuration
jwt.secret=nirmalahospitaljwtsecretkey2026forsecuringbackendapi!
jwt.expiration=86400000

# File Upload limits
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
